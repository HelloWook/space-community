'use client';

import { Scene } from '@/shared/ui/Scene';

// 홈 페이지 - 전체 화면 3D 장면
export function HomePage() {
  return (
    <div style={{ width: '100vw', height: '100vh' }}>
      <Scene />
    </div>
  );
}
